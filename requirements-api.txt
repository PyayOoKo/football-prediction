# Football Prediction API — Python dependencies
# Install with: pip install -r requirements-api.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Validation
pydantic>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.26.0

# ML
scikit-learn>=1.5.0
joblib>=1.3.0

# HTTP client (for proxy/integration)
httpx>=0.27.0

# CORS middleware (included with FastAPI)

# Rate limiting (built-in, in-memory)
# For distributed rate limiting, add: redis>=5.0.0
