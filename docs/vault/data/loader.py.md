---
tags:
  - python-module
  - data
  - loader
---

# `data/loader.py` — Data Loader (subpackage)

**Path:** `src/data/loader.py`

Modern data loader within the `src/data/` subpackage. Reads cleaned CSV/Parquet, handles caching, schema validation, encoding detection, and large-file chunking.

**Key class:** `DataLoader` — load, load_cached, validate_schema, preview

See also: [[data/cleaners.py]], [[data_loader.py]], [[data/feature_engineering.py]], [[Config System]]
